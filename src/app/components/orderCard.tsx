import React from 'react';

type Order = {
  // Define the properties of your order object
}

const OrderCard = (order: Order) => {
  return (
    <div >
      {/* This contains an order already requested*/}
    </div>
  );
};

export default OrderCard;